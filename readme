# Mini-OBS — 边缘原生轻量级日志管理系统

[![Rust](https://github.com/your-org/mini-obs/actions/workflows/rust.yml/badge.svg)](https://github.com/your-org/mini-obs/actions/workflows/rust.yml)
![Version](https://img.shields.io/badge/version-0.2.0-blue)
![License](https://img.shields.io/badge/license-Apache--2.0-green)
![Rust](https://img.shields.io/badge/rust-2021%20edition-orange)

## 1. 项目简介

Mini-OBS 是一个面向**资源受限边缘设备**（树莓派 4B 为基准硬件）的轻量级日志管理系统。

**核心目标**：在边缘侧实现 **>10x 压缩存储 + 秒级本地查询 + 崩溃自动恢复**，同时规避微软 ANS 专利（US11234023B）。

### 核心特性

- 🗜️ **高压缩比**：Delta 时间戳 + 模板提取 + XOR-P 编码 + Zstd 字典压缩，实测 >10x
- 🔍 **秒级查询**：内存索引 + Segment mmap + ChunkSummary 两阶段过滤，避免全量解压
- 🔒 **专利安全**：不直接实现 rANS/tANS，通过预处理降熵后调用标准 Zstd 库（内部封装 ANS/FSE）
- ⚡ **崩溃恢复**：文本 WAL 保证断电不丢数据，启动时自动重放恢复
- 🧩 **模板压缩 v2**：Batch 级模板提取 + 强类型参数（int/hex/IPv4/BlockId）+ 64-bit XOR-P 编码
- 🎯 **多源采集**：文件 tail、stdin、模拟生成器
- 🐳 **Docker 友好**：支持 x86_64 / aarch64 交叉编译

---

## 2. 快速开始

### 构建

```bash
# 依赖：Rust 1.70+ (edition 2021)
cargo build --release

# 交叉编译（树莓派 4B）
rustup target add aarch64-unknown-linux-gnu
cargo build --release --target aarch64-unknown-linux-gnu
```

### 运行测试

```bash
# 全部测试（147 个）
cargo test

# 按模块测试
cargo test agent::storage
cargo test agent::compressor
cargo test agent::template
cargo test agent::index
cargo test agent::collector
cargo test shared::format
```

### CLI 使用

```bash
# 生成测试日志
./target/release/mini-obs generate 10000 /tmp/test.log my-service

# 启动采集代理（tail 模式）
./target/release/mini-obs agent /data/mini-obs nginx /var/log/nginx/access.log

# 查询日志
./target/release/mini-obs query /data/mini-obs 0 9999999999999 ERROR 50

# 查看统计
./target/release/mini-obs stats /data/mini-obs
```

---

## 3. 目录结构

```
mini-obs/
├── Cargo.toml                         # 项目配置 (v0.2.0, edition 2021)
├── readme                             # 本文件
├── .github/workflows/rust.yml         # CI：构建 + 测试
├── src/
│   ├── main.rs                        # CLI 入口（agent / query / generate / stats）
│   ├── lib.rs                         # 库入口 + 测试工具
│   ├── agent/
│   │   ├── mod.rs                     # 模块聚合，重新导出公共类型
│   │   ├── collector.rs               # 采集引擎（file tail / stdin / mock）
│   │   ├── compressor.rs              # 压缩引擎（Delta+RLE+服务引用+Zstd+字典）
│   │   ├── template.rs                # 🆕 模板提取 + XOR-P 参数编码
│   │   ├── index.rs                   # 内存索引 + 磁盘 Manifest
│   │   └── storage.rs                 # 存储引擎（WAL + v2 多 Chunk Segment）
│   └── shared/
│       ├── mod.rs
│       └── format.rs                  # MOBS 二进制格式契约（v1 + v2 兼容）
├── tests/
│   ├── integration_test.rs            # 集成测试
│   ├── integration_test2.rs           # 集成测试（扩展）
│   ├── compression_effectiveness.rs   # 压缩效果基准测试
│   ├── compressor_standalone.rs       # 压缩器独立测试
│   ├── template_diagnostic.rs         # 模板诊断测试
│   └── gen_logs.py                    # 测试日志生成脚本
└── docs/                              # 文档
```

---

## 4. 架构概览

```
┌──────────────────────────────────────────────────────────────┐
│                       main.rs (CLI)                           │
│           agent / query / generate / stats                    │
└───────────────────────────┬──────────────────────────────────┘
                            │
┌───────────────────────────▼──────────────────────────────────┐
│                     agent/mod.rs                               │
│     重新导出：Collector, Compressor, Index, StorageEngine       │
└───────────────────────────┬──────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
┌───────▼──────┐  ┌─────────▼──────┐  ┌────────▼──────┐
│  collector   │  │  template.rs   │  │    index       │
│  采集层       │  │  模板提取      │  │   索引层       │
│  生产者线程   │  │  XOR-P 编码    │  │   内存+磁盘    │
└───────┬──────┘  └─────────┬──────┘  └────────┬──────┘
        │                   │                   │
        └───────────────────┼───────────────────┘
                            │
                   ┌────────▼────────┐
                   │  compressor     │
                   │  Delta+RLE+Zstd │
                   └────────┬────────┘
                            │
                   ┌────────▼────────┐
                   │    storage       │
                   │  WAL + Segment   │
                   │  v2 多Chunk格式  │
                   └────────┬────────┘
                            │
            ┌───────────────┴───────────────┐
            │                               │
┌───────────▼──────────┐     ┌──────────────▼──────────┐
│   shared/format.rs   │     │     Zstd 标准库           │
│   MOBS 二进制格式     │     │    （专利安全封装）        │
│   v1 + v2 兼容        │     │                          │
└──────────────────────┘     └─────────────────────────┘
```

---

## 5. 模块说明

### 5.1 `shared/format.rs` — 二进制格式契约

定义 MOBS 存储格式的所有结构体与常量。

| 结构体 | 大小 | 说明 |
|--------|------|------|
| `SegmentHeader` | 64 B | 魔数 `MOBS` + 版本 + chunk_count + created_at + segment_id + CRC32 |
| `ChunkEntry` | 32 B | offset + compressed_sz + original_sz + line_count + min_ts + max_ts |
| `ChunkSummary` | 80 B | 🆕 v2 新增：min/max_ts + line_count + pattern_mask + param_bloom |
| `SegmentFooter` | 8 B | chunk_table_offset + content_crc32 |
| `ManifestEntry` | 64 B | segment_id + min/max_ts + line_count + path_hash + flags |
| `ManifestHeader` | 9 B | 魔数 `MIDX` + version + entry_count |
| `LogLine` | — | `{"t":ts,"s":"svc","l":"E","m":"msg"}` |

所有结构体使用 `#[repr(C)]` + 小端序，保证跨平台（x86_64 / aarch64）mmap 零拷贝解析。

### 5.2 `agent/collector.rs` — 采集引擎

- **来源支持**：文件 tail、stdin、模拟生成器（Mock）
- **解析策略**：JSON 对象 → Nginx 启发式 → 关键词降级
- **线程模型**：独立采集线程 + `AtomicBool` 优雅停止 + mpsc channel 传递 `LogLine`

### 5.3 `agent/template.rs` — 模板提取与 XOR-P 编码 🆕

v0.2.0 新增模块，实现三层压缩预处理：

1. **模板提取**：Batch 内按 token 数 + 结构签名聚类，自动识别参数位置
   - 退化检测：当固定 Literal 过少时，按 token 类型模式子分组，防止不同消息类型错误合并
   - 单条 fallback：无法聚类时作为完整 Literal 模板保留原始字符
2. **强类型参数编码**：自动识别并二进制编码常见日志参数
   - `Integer`（i64 LE）、`Hex`（u64 LE）、`IPv4`（4 B）、`IPv4Port`（6 B）、`BlockId`（9 B）、`Timestamp`（4 B）、`Path`
   - 严格往返一致性保证：前导零、符号等边界情况全处理
3. **64-bit XOR-P 编码**：ARM64 字长对齐，bitmap + literals 紧凑格式
   - 参考行间隔可配置（默认 16 行），查询友好
   - 参数数量/类型变化时自动重置参考行
4. **PatternTable 序列化**：TLV 格式，支持磁盘持久化与零拷贝反序列化

### 5.4 `agent/compressor.rs` — 压缩引擎

- **预处理流水线**：Delta 时间戳 → 行间 RLE（`"*"` 引用上一条） → 服务名引用（`"$n"`）
- **Zstd 压缩**：可配置压缩级别（0-22），支持训练字典
- **接口**：`compress_batch()` / `decompress_batch()`，透明处理降熵/还原

### 5.5 `agent/index.rs` — 索引引擎

- 内存常驻 Segment 元数据（`RwLock<Vec<ManifestEntry>>`）
- 磁盘 Manifest 原子保存（`index/manifest.midx`）
- 自愈：损坏时自动扫描 `segments/` 目录从文件头重建
- 内存 <1 MB / 万条 Segment

### 5.6 `agent/storage.rs` — 存储引擎

**写入流水线（v2 多 Chunk）：**
```
append -> WAL 追加（文本 json\n）
       -> 内存缓冲
       -> 阈值触发 flush()
           -> drain 缓冲
           -> TemplateExtractor::extract() 模板提取
           -> TemplateExtractor::encode_xor() XOR-P 编码
           -> 按 chunk_size（默认 256 行）切分 Chunk
           -> 每 Chunk 计算 ChunkSummary（时间范围 + pattern_mask + param_bloom）
           -> compressor.compress_batch() Zstd 压缩
           -> 组装 Segment（PatternTable + ChunkTable + SummaryTable + Data）
           -> 原子写入（tmp -> rename）
           -> index.add_segment()
           -> WAL truncate
```

**查询流水线（两阶段过滤）：**
```
query(start, end, keyword, limit)
  -> index.query_range(start, end)           # 时间过滤，O(log N)
  -> 遍历 Segment（最新优先）
     -> mmap Segment 文件
     -> 读取 PatternTable, 构建 keyword -> pat_ids 映射
     -> 遍历 ChunkSummary：
        -> 时间范围过滤
        -> pattern_mask 过滤（模板无关键字→跳过）
        -> param_bloom 过滤（参数不匹配→跳过）
     -> 仅对命中 Chunk 解压 + XOR-P 还原
     -> 逐行关键词子串匹配
```

**崩溃恢复：** 启动时读取 `wal/current.wal`，逐行重放→立即 `flush()`→截断 WAL

---

## 6. 存储目录布局

```
/var/lib/mini-obs/              # data_dir
├── wal/
│   └── current.wal             # 文本 JSON Lines，未 flush（<5 MB）
├── segments/
│   ├── segment-00000001.mobs   # 只读压缩段（MOBS 格式）
│   ├── segment-00000002.mobs
│   └── ...
└── index/
    └── manifest.midx           # 固定大小 Entry 数组
```

---

## 7. 依赖项

| Crate | 版本 | 用途 |
|-------|------|------|
| `serde` + `serde_json` | 1.0 | 日志行 JSON 序列化 |
| `zstd` | 0.13 | 压缩/解压（内部封装 ANS/FSE，专利安全） |
| `crc32fast` | 1.3 | CRC32 校验（IEEE 802.3 多项式） |
| `memmap2` | 0.9 | 零拷贝文件映射 |
| `bincode` | 1.3 | 二进制序列化 |

---

## 8. 关键设计决策

| 决策 | 理由 | 替代方案及未采用原因 |
|------|------|----------------------|
| Zstd 而非原生 ANS | 规避微软专利 US11234023B（2022-2039 有效） | rANS/tANS：压缩率更高但法律风险 |
| 文本 WAL 而非二进制 WAL | 崩溃时可人工修复，调试友好 | 二进制 WAL：体积小但不可读 |
| 多 Chunk Segment（v2） | 段内时间切片 + ChunkSummary，查询时跳过无关 Chunk | 单 Chunk：简单但解压全段 |
| JSON Lines 而非 MessagePack | 人类可读，调试效率优先 | MessagePack：体积小 5% 但失去可读性 |
| 内存索引而非磁盘 B-Tree | 万级 Segment <1 MB，查询快 | SQLite/RocksDB：引入外部依赖 |
| 模板提取 + XOR-P 编码 | 利用日志模板化特性进一步压缩，强类型参数更紧凑 | 纯 Zstd：无视日志领域特性 |
| Mutex 而非 lock-free | 单线程写入假设，简单可靠 | crossbeam：过度设计 |

---

## 9. 测试状态

| 模块 | 测试数 | 状态 |
|------|--------|------|
| `shared::format` | CRC + roundtrip + 对齐 | ✅ 全部通过 |
| `agent::index` | 持久化 + 重建 + 范围查询 | ✅ 全部通过 |
| `agent::collector` | 多格式解析 + mock 速率 + 背压 | ✅ 全部通过 |
| `agent::compressor` | roundtrip + 压缩比 + 字典 + 模板集成 | ✅ 全部通过 |
| `agent::template` | 类型检测 + XOR-P 往返 + 结构分组 + 边界 | ✅ 全部通过 |
| `agent::storage` | 写-读-恢复 + 多 Segment + 高频追加 | ✅ 全部通过 |
| 集成测试 | 端到端 + 压缩效果 + 模板诊断 | ✅ 146/147 通过 (1 个需外部数据文件) |

```bash
# 测试结果（最近一次）
test result: ok. 147 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.63s
```

---

## 10. 待办事项

### 高优先级
- [ ] 树莓派 4B 性能基准测试（1000 QPS 稳定性、内存 RSS、压缩比）
- [ ] 与 Loki / Fluent Bit 同数据集对比报告
- [ ] Dockerfile + docker-compose.yml（含 aarch64 交叉编译）

### 中优先级
- [ ] HTTP API（axum）：`/query?start=&end=&keyword=` REST 接口
- [ ] Prometheus Remote Write 兼容
- [ ] Segment 后台合并（compaction）

### 低优先级
- [ ] eBPF 采集器（绕过文件 IO）
- [ ] ARM NEON SIMD 加速
- [ ] 自适应 Chunk 大小（根据数据特征动态调整）

---

## 11. 时间线

| 提交 | 内容 |
|------|------|
| 2026-06-14 | 模板测试完善，修复模板匹配逻辑，二进制模板 |
| 2026-06-14 | 批量测试，修复中断重启 bug |
| 2026-05~06 | 多轮测试迭代 |
| 2026-05-15 | 重建索引层，重构存储逻辑 (v2 多 Chunk) |
| 2026-05-15 | GitHub Actions CI 配置 |
| 2026-05-14 | v0.2.0 完善压缩策略，模板模块 |
| 2026-05-14 | 完成 main.rs CLI |
| 2026-05-13 | 重构日志采集与压缩引擎 |
| 2026-05-12 | 项目初始化 |

---

## 12. 许可证

Apache-2.0 — 参见 [Cargo.toml](Cargo.toml)

---

*Mini-OBS: 把大厂的日志能力带到边缘，一个树莓派就够了。*
